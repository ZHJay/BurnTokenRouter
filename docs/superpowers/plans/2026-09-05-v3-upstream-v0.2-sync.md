# v3 Upstream v0.2 Sync Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fast-forward the fork main to upstream b1748c4ea and rebase the v3 Apple frontend onto it with every upstream v0.2 feature reachable and visually integrated.

**Architecture:** Preserve upstream behavior, API contracts, fields, routes, and tests while retaining v3 ownership of GlobalNav, layout, tokens, responsive behavior, and presentation. Use a linear rebase with immutable safety refs, resolve conflicts at the behavior/presentation boundary, validate the merged product, then update v3 with an explicit lease.

**Tech Stack:** Git, Vue 3, TypeScript, Pinia, Vue Router, Tailwind CSS, Vitest, Vue Test Utils, vue-tsc, Vite, Go.

## Global Constraints

- Upstream target is b1748c4ea99ce2120401a269142aa071e18a84da unless a later fetch is explicitly accepted.
- Preserve all 157 current untracked duplicate files; baseline aggregate SHA-1 is f7ad501bb8a07a70c34748dc539f1d85a3bd28eb.
- Do not add dependencies or create a second component/design system.
- Local and fork main update only by fast-forward; never rewrite main.
- Remote v3 updates only after verification and only with an explicit force-with-lease.
- Keep AppSidebar deleted; GlobalNav and navItems remain the navigation source of truth.

---

### Task 1: Freeze Safety State and Establish Baseline

**Files:**
- Read: `frontend/package.json`
- Read: `docs/superpowers/specs/2026-09-05-v3-upstream-v0.2-sync-design.md`
- No production changes.

**Interfaces:**
- Consumes: local v3 a93e1665d, remote v3 0a094f355, upstream b1748c4ea.
- Produces: backup branch plus local and remote safety refs.

- [ ] **Step 1:** Re-fetch both remotes and fail if upstream/main differs from b1748c4ea without review.
- [ ] **Step 2:** Verify tracked changes equal zero, untracked count equals 157, and aggregate hash equals f7ad501bb8a07a70c34748dc539f1d85a3bd28eb.
- [ ] **Step 3:** Create `backup/apple-liquid-glass-v3-pre-v020-20260905`, `refs/safety/v3-local-pre-v020`, and `refs/safety/v3-origin-pre-v020`.
- [ ] **Step 4:** Run the current v3 navigation, model-plaza, CN quota, Groups, Usage, and Use Key focused tests to record a passing pre-rebase baseline or isolate pre-existing failures.
- [ ] **Step 5:** Commit no generated output and confirm the protected-file aggregate hash is unchanged.

### Task 2: Fast-forward Local and Fork Main

**Files:**
- No file edits; ref movement only.

**Interfaces:**
- Consumes: `upstream/main` b1748c4ea and local/origin main d45135d87.
- Produces: local main and origin/main at b1748c4ea.

- [ ] **Step 1:** Check out main and run `git merge --ff-only upstream/main`.
- [ ] **Step 2:** Verify main equals upstream/main and is a descendant of the old d45135d87 commit.
- [ ] **Step 3:** Push `main:main` to origin without force.
- [ ] **Step 4:** Fetch origin and verify local main, origin/main, and upstream/main are identical.

### Task 3: Rebase v3 and Resolve Structural Conflicts

**Files:**
- Modify: `.gitignore`
- Modify: `backend/internal/service/update_service.go`
- Modify: conflicted frontend files reported by Git.
- Delete/keep deleted: `frontend/src/components/layout/AppSidebar.vue`

**Interfaces:**
- Consumes: v3 commits after d45135d87 and updated main.
- Produces: linear v3 history with main as ancestor and no obsolete sync merges.

- [ ] **Step 1:** Check out v3 and run `GIT_EDITOR=true git rebase main`.
- [ ] **Step 2:** For each stop, inspect stage 1/2/3 and all callers before editing; retain the meaningful gitignore union and both upstream updater semantics plus the fork version suffix.
- [ ] **Step 3:** Keep AppSidebar deleted and retain GlobalNav/navItems; do not resurrect the legacy sidebar.
- [ ] **Step 4:** Resolve frontend conflicts by keeping upstream data/behavior and v3 markup/tokens; keep locale key unions and all behavioral test assertions.
- [ ] **Step 5:** Continue until complete, then assert main is an ancestor, `git rev-list --merges main..HEAD` is empty, and no conflict markers remain.
- [ ] **Step 6:** Recheck the protected untracked count and hash before any integration edits.

### Task 4: Integrate New Navigation and Plugin Management

**Files:**
- Modify: `frontend/src/components/layout/navItems.ts`
- Modify: `frontend/src/components/layout/GlobalNav.vue` only if icon/rendering support is needed.
- Modify: `frontend/src/views/admin/PluginsView.vue` only for v3 token/primitive alignment.
- Test: `frontend/src/components/layout/__tests__/GlobalNav.spec.ts`
- Test: `frontend/src/views/admin/__tests__/PluginsView.spec.ts`

**Interfaces:**
- Consumes: upstream `FeatureFlags.pluginManagement`, `/admin/plugins`, and locale key `nav.plugins`.
- Produces: feature-gated plugin item in the v3 admin navigation and a v3-consistent management page.

- [ ] **Step 1:** Add a failing navigation test that enables plugin management and expects exactly one `/admin/plugins` entry, then disables it and expects none.
- [ ] **Step 2:** Extend `NavFlags` with `pluginManagement` and insert `/admin/plugins` after `/admin/accounts` in `buildAdminNavItems`.
- [ ] **Step 3:** Wire the existing public-settings feature flag in GlobalNav and use the existing icon fallback/pattern.
- [ ] **Step 4:** Run GlobalNav and PluginsView tests; make only the minimal styling changes required for existing v3 tokens, dark mode, and narrow layouts.
- [ ] **Step 5:** Commit navigation/plugin integration as one logical change.

### Task 5: Reconcile Accounts, Usage, Groups, and Pricing

**Files:**
- Modify as needed: `frontend/src/components/account/CNProviderQuotaCell.vue`
- Modify as needed: `frontend/src/components/account/UsageProgressBar.vue`
- Modify as needed: `frontend/src/components/admin/usage/UsageStatsCards.vue`
- Modify as needed: `frontend/src/components/modelPlaza/PlazaGroupSection.vue`
- Modify as needed: `frontend/src/components/modelPlaza/PlazaModelPricingTable.vue`
- Modify as needed: `frontend/src/views/admin/GroupsView.vue`
- Test: adjacent existing `*.spec.ts` files and upstream integration tests.

**Interfaces:**
- Consumes: upstream priority, reasoning effort, compaction, Fast, quota window, weekday pricing, multiplier, and long-context fields.
- Produces: all fields rendered through v3 components without removing existing card/table modes.

- [ ] **Step 1:** Run focused upstream tests for CN quota, account usage, requested reasoning effort, compaction, group Fast/reasoning policies, time pricing, and model-plaza pricing; group failures by root component.
- [ ] **Step 2:** For each behavior failure, retain the upstream property names and events and adapt only its markup/classes to the existing v3 component pattern.
- [ ] **Step 3:** Add one focused regression assertion where auto-merge omitted a reachable field or preserved legacy-only presentation; verify it fails before the production edit.
- [ ] **Step 4:** Run the complete focused set until green and commit one logical integration group at a time.

### Task 6: Audit Remaining Upstream Frontend Features

**Files:**
- Review: all 132 upstream-changed frontend paths.
- Modify only files with a demonstrated reachability, behavior, or visual integration gap.
- Test: existing upstream tests for each affected feature.

**Interfaces:**
- Consumes: upstream route/flag/API diff and v3 changed-path diff.
- Produces: a checked feature matrix for Codex catalog/key guidance, reset cards, reauthorization, Zhipu/Ollama quotas, public-group restrictions, IPv6 proxies, strict local dates, payments, promo codes, and Ops details.

- [ ] **Step 1:** Map every user-visible upstream commit to its route, API/type, component/view, locale keys, and existing test.
- [ ] **Step 2:** Confirm route reachability through GlobalNav or the owning workflow; add no extra navigation where upstream intentionally exposes a modal/inline flow.
- [ ] **Step 3:** Run each owning test and inspect changed controls for v3 tokens, dark mode, and responsive containment.
- [ ] **Step 4:** Fix only demonstrated gaps, leaving automatically integrated behavior untouched when it already satisfies the contract.

### Task 7: Full Verification and Rendered QA

**Files:**
- No intended production edits after checks start; failures return to the owning task.

**Interfaces:**
- Consumes: rebased integrated v3.
- Produces: test, type, build, backend, and browser evidence.

- [ ] **Step 1:** Run the complete frontend Vitest suite with no update mode.
- [ ] **Step 2:** Run `vue-tsc --noEmit` and the production Vite build.
- [ ] **Step 3:** Run backend tests for update-service/version behavior and every backend package manually conflict-resolved; run broader standard checks if a shared package changed.
- [ ] **Step 4:** Launch the existing frontend and perform desktop/mobile, light/dark rendered checks for GlobalNav/plugins, Groups, Accounts, Usage, Model Plaza, and Use Key Codex guidance.
- [ ] **Step 5:** Confirm no relevant console errors, overlays, clipping, inaccessible controls, or missing translations.
- [ ] **Step 6:** Run `git diff --check`, verify tracked cleanliness after commits, and verify the protected untracked count/hash.

### Task 8: Publish Verified v3

**Files:**
- No file edits; remote ref update only.

**Interfaces:**
- Consumes: verified HEAD and `refs/safety/v3-origin-pre-v020`.
- Produces: origin v3 equal to verified local v3.

- [ ] **Step 1:** Fetch origin and confirm its v3 ref still equals the recorded safety ref.
- [ ] **Step 2:** Re-run ancestry, no-merge, tracked-cleanliness, and untracked hash gates.
- [ ] **Step 3:** Push HEAD to origin v3 using an explicit force-with-lease tied to the recorded SHA.
- [ ] **Step 4:** Fetch origin and verify remote v3 equals HEAD and contains upstream/main.
- [ ] **Step 5:** Retain the backup branch and report exact hashes, tests, feature coverage, and any non-blocking limitations.
